# Codex CLI 简体中文使用说明

1. 先核对 `SHA256SUMS.txt`。
2. 退出正在运行的目标 CLI。
3. 在 PowerShell 中运行 `./Install.ps1`。
4. 安装脚本只切换 `codex-cli.cmd`，不修改其他 CLI 入口、账号或认证文件。
5. 卸载并恢复官方入口时运行 `./Uninstall.ps1`。

汉化版必须与官方 Codex CLI 版本精确对应。不支持的版本会拒绝安装。
